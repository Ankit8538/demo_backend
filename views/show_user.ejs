<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all user data display</title>
    <style>
        table,th,td{
            border: 0.5px solid red;
        }
    </style>
</head>
<body>
    <h1>Accoding to table</h1>
    <table>
        <tr>
            <th>id</th>
            <th>username</th>
            <th>Email_id</th>
        </tr>
        <% for(user of users) { %>
            <tr>
                <td><%=user.id %></td>
                <td><%=user.username %></td>
                <td><%=user.email%></td>
            <td>
            <form method="get" action="/user/<%=user.id%>/edit">
                <button>Edit username</button>
            </form>
            </td>
           </tr>
            <% } %>
        
    </table>
    
</body>
</html>